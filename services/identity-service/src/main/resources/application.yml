spring:
  application:
    name: identity-service
  config:
    import: optional:configserver:http://localhost:8888

server:
  port: 8082

# Logging
logging:
  level:
    com.surework.identity: DEBUG
